# TalentScope frontend

React + Vite + TypeScript + Tailwind CSS frontend for the AI Resume
Evaluator. See the [project root README](../README.md) for setup,
environment variables, and how this connects to the backend.

```bash
npm install
npm run dev
```
